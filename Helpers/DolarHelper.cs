namespace TrabajoFinalSofttek.Helpers
{
    public class DolarHelper
    {
        private IConfiguration _configuration;
        public DolarHelper(IConfiguration configuration)
        {

            _configuration = configuration;
        }
    }
}
